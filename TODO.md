# 1、时间戳调度

每个 tick 进一次 systick 中断跑一次嘀嗒虽然没有多大开销，但是 tickless 的时候需要动态调整 systick 寄存器倒计时什么的，还是不便于移植。  
应该让嘀嗒能够返回下次唤醒的毫秒数，把配置硬件定时器的操作移到外部。  

并且还要有时间戳调度能力，可以不需要 systick 中断运行嘀嗒函数，调度器取 poll 系统时间戳来决定是否弹出。

# 2、空闲与 tickless 结构优化

V3 的设计是在开启空闲任务后将调度器运行在一个最低优先级的软中断（如 pendsv）里面，当事件发布时，同时会触发这个软中断，事件也会在软中断内弹出和处理，从而在主循环中提供一个真正的最低优先级空闲任务。  
这套机制有点多此一举了，应该会删除将调度器跑在软中断的逻辑，空闲任务没有什么必要，调度器应该还是得跑在主循环里比较好，留出空闲钩子给用户做低功耗操作就行了。

# 3、多核心/多线程支持

异构多核 AMP 不打算支持，用户自己跑两个调度器实例就行了。  

同构多核 SMP 规划中。  
目前的调度器逻辑是主循环不断弹出事件队列的队列头，在任务和中断中都可以发布事件，对于单核，原子操作则只需要关开中断即可。  
对于多核，则可以将调度器事件循环设计为多核可重入的，也就是两个核心同时弹出事件队列头，谁空闲了谁就弹出并执行回调，自动均衡负载。  

所以这个事件队列的工作情况如下：
* 消费者：每个核心的主循环都去原子地弹出队列头，然后非原子执行事件回调
* 生产者：中断以及任务都可以原子地将事件推入事件队列

那么对于事件循环的改造设想如下：

**1、队列原子操作需要考虑多核竞争**

上锁需要先关中断（一般只能关自己核心的中断），然后获取队列自旋锁，操作完成后释放自旋锁，再开中断。

关中断是为了避免某个核的任务与中断竞争，自旋是为了避免多核竞争。  
中断态也可以自旋，因为首先已经关中断了，所以不会被自己核的更高优先级中断抢断死锁，而且另一个核获取锁之后很快就会释放锁，不会耽误这边的中断太久。

**2、避免重入事件回调**

弹出事件对象是原子的，但是执行回调不可能是原子的，假设此时 核心0 弹出 事件a，并正在执行它的回调，但是此时中断或者任务又发布了一次 事件a，那么可能会被空闲的 核心1 弹出，那么就会出现双核并行执行同一个回调，出现不可预料的问题。

所以需要标记某个事件是正在处理中的，此时另一个核心在弹出事件时应该尝试绕过它弹出下一个事件，或者再次发布时不入队，而是给这个事件一个标记，让它在执行完后调度器帮它再次入队

**3、事件订阅破坏链表**

执行回调时会遍历订阅者链表，单核情况下某个订阅者取消订阅或者新来一个订阅者不会有问题，操作这个链表是非原子的，但是多核下需要考虑对这个链表的原子操作，可能带来额外的开销。

**4、其它竞争资源**

本项目作为裸机调度器，本质协作式调度，在单核无任务抢占的情况下，业务几乎不需要考虑到变量原子操作。但是一旦拓展到多核之后，业务上就必须考虑到更多情况下的资源竞争，比如下游组件 ctx，在设计上就没有考虑多核的情况，即便 ltx 支持了多核，ctx 也不能直接使用，比如 `wait_topic` 这个重要组件用的是同一个变量给所有任务标记当前事件是否触发，因为事件回调执行完毕之前，不会有任何其它任务去操作这个变量。
